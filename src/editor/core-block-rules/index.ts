/**
 * Core Block Validation Rules — editor bundle.
 *
 * Imports every per-block validator. Each file uses `addFilter` to hook
 * `editor.validateBlock` or `editor.validateEditor` from the framework.
 */

import './image';
import './button';
import './table';
import './heading';
import './gallery';
import './post-title';
